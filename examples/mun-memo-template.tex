\documentclass[$options$]{mun-memo}

%
% Pandoc output expects this command to exist:
%
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

\usepackage{setspace}

\title{$title$}
\author{$author$}

$if(address)$
\address{$address$}
$endif$

$if(returnaddress)$
\returnaddress{$returnaddress$}
$endif$

$if(closing)$
\closing{$closing$}
$endif$

\begin{document}
\maketitle

$body$

\end{document}
